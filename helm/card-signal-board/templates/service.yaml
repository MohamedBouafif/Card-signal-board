apiVersion: v1
kind: Service
metadata:
  name: {{ include "card-signal-board.fullname" . }}
  labels:
    {{- include "card-signal-board.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "card-signal-board.selectorLabels" . | nindent 4 }}
