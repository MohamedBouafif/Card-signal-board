apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "card-signal-board.fullname" . }}-config
  labels:
    {{- include "card-signal-board.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
